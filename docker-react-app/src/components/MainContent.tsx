import React from 'react';

const MainContent: React.FC = () => {
  return (
    <main>
      <p>This is a simple docker app with a header, main content, and footer.</p>
    </main>
  );
};

export default MainContent;
